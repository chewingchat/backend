spring:
  config:
    activate:
      on-profile: test

openai:
  model: test
  api:
    url: test-url
    key: test-key

---
spring:
  config:
    activate:
      on-profile: local

openai:
  model: local
  api:
    url: local-url
    key: local-key

---
spring:
  config:
    activate:
      on-profile: live

openai:
  model: ${OPENAI_API_MODEL}
  api:
    url: ${OPENAI_API_URL}
    key: ${OPENAI_API_KEY}
